###### Aplicação apenas descreve de maneira simplificada o funcionamento do ciclo de vida utilizando React Components
#### Para executar, clone o repositório e execute os comandos abaixo

`npm install`

`npm start`